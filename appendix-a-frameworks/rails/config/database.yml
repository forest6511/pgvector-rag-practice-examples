default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  host: <%= ENV.fetch("DATABASE_HOST") { "localhost" } %>
  port: <%= ENV.fetch("DATABASE_PORT") { 5432 } %>
  username: <%= ENV.fetch("DATABASE_USER") { "rag" } %>
  password: <%= ENV.fetch("DATABASE_PASSWORD") { "ragpass" } %>

development:
  <<: *default
  database: ragdb

test:
  <<: *default
  database: ragdb_test

production:
  <<: *default
  database: ragdb
